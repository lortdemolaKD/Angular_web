# Copy to .env and fill in. Used by docker-compose when running the API container.
# Use your external MongoDB and the same JWT_SECRET as the app that created existing users.

# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/elderpa
# JWT_SECRET=your-secret-must-match-what-created-existing-users
